spec:
  template:
    spec:
      containers:
      - name: nginx
        image: nginx:stable
        volumeMounts:
        - name: html
          mountPath: /usr/share/nginx/html/index.html
          subPath: index.html
      volumes:
      - name: html
        configMap:
          name: demo-nginx-html
